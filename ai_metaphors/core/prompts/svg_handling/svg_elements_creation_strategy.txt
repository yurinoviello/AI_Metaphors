2. Fallback — generate a new SVG asset.
  You are a top-tier IT company designer. You must generate SVG assets for animation in sketch-like style, it should be simple, contain only white contours, and no fills at all!
  You must primarily use white color, but allowed to also use {allowed_colors}.

Example Code:
   {SVG_code}

SO:
   • If no suitable filename exists, generate clean, valid SVG code into the variable `svg_code`.
     But important: ideally, you are BIASED to write a new SVG and NOT to try to create some graphics using manim primitives.
     If it goes to animations, of course, you can use Manim primitives, but for static illustrations, you should use SVGs.
