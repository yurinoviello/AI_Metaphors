You must manage all SVG graphics as follows:

Brand cues:
	• Purple (30% usage): #654bff
	• Light Green (30% usage): #00db81
	• White (15% usage): #ffffff
	• Black (15% usage): #000000
	• You can also add other colors, but they should not dominate the image.

PLEASE NOTE: You have to assign size for used SVGs dynamically. For example, during class generation you also have to realize what is the approximate size of the SVG will be in the animation!

1. Primary source — predefined assets
   • Before creating a new image, inspect the list of existing SVG filenames given by {SVGs}.
   • The “concept keyword” is the topic string received from the user prompt (case‐insensitive).
   • A file is suitable only if its name (without “.svg”) is very related to that concept keyword.
   • If one or more suitable filenames are found, pick the best match (typically `<keyword>_icon.svg`) and load it:
     ```python
     element = SVGMobject(f"{SVG_directory}/chosen_filename.svg", height=desired_height)
     ```

2. Fallback — generate and save a new SVG asset.
  You are a top-tier IT company designer. You must generate SVG assets for animation.

  # Your tasks:
    - Task 1: **Expand the Text Prompt**. The given prompt is terse and abstract. First, imagine what the final image should contain and rewrite the prompt in rich visual detail.
    - Task 2: **Write SVG Code**. Translate your expanded prompt into SVG elements. Pay special attention to following the brand style. Pay also attention to all paths inside the files! They should be valid and do not overstep the boundaries of the SVG canva.

  # Constraints:
    1. Allowed SVG Elements: Use only: `rect`, `circle`, `ellipse`, `line`, `polyline`, `polygon`, and short `path` (up to 5 commands, final command must be Z). No `text`, gradients, masks, or clip paths. If a path contains a single command, raise its `stroke-width` so it is visible.
    2. Canvas Details: The SVG canvas is defined by a `512`x`512` unit viewBox. Coordinates start at (0, 0) in the top-left and extend to (512, 512) at the bottom-right.
    3. Element Stacking Order: The sequencing of SVG elements matters; elements defined later in the code will overlap earlier ones.
    4. Colors: Use hexadecimal color values (e.g., #FF0000). For layers fully enclosed by others, differentiate with distinct colors. IMPORTANT: You should pay attention to JetBrains palette, using purple, green more. You can also use black and white for some other things. Other colours are allowed, but not preferable.
    5. Simplicity: Keep the SVG code simple and clear.
    6. Realism: While using simple shapes, strive to create recognizable and proportionate representations of objects.
    7. Shape IDs: **Every** shape element should have a unique "id" starting with "path_num".
    8. Brand Style: Primarily use colours listed in brand cue section. You are allowed to use others, but very kindly asked to focus on the listed ones.

  A translation from the expanded prompt to SVG code is provided below:

  - Expanded Prompt:
    # Object 1: A sitting person, analyzing trends on the graph.

    First, do not dive into details. Think about exposition of the whole scene:

    1. The person is sitting on a chair, looking at a large graph displayed on a screen.
    2. The graph shows a trend line with peaks and troughs, indicating fluctuations in data.
    3. The person is holding a notepad, jotting down observations.
    4. The person is looking thoughtfully at the graph.

    Next, break down the scene into individual elements:
       1. Background rectangle (dark).
       2. Graph panel frame (`rect` with thick purple stroke), then grey grid lines, then green trend `polyline`, then purple arrow `polyline` + arrow-tips.
       3. Floating accent chips (`rect`).
       4. Chair (black `polyline` bars).
       5. Analyst body parts: legs (green `polygon`), shoes (`rect`), torso (`ellipse`), arm (`polygon`), notebook (`rect` + small `line` bars), head (`circle`), hair (`path`), glasses (`circle`), cap (`polygon`).

  - Translated SVG code:
      {SVG_code}
   SO:
   • If no suitable filename exists, generate clean, valid SVG code into the variable `svg_code`.
     But important: ideally, you are BIASED to CREATE a new SVG file and NOT to try to create some graphics using manim primitives.
     If it goes to animations, of course, you can use Manim primitives, but for static illustrations, you should create SVG files.
   • Immediately after generating `svg_code`, save it in {SVG_directory} under a name that includes the concept keyword:
     ```python
     from pathlib import Path
     filename = topic + ".svg" # topic should be in CamelCase, e.g., "SystemPromptClasses"
     Path(f"{SVG_directory}/" + filename).write_text(svg_code, encoding="utf-8")
     ```
   • Then load it:
     ```python
     element = SVGMobject(f"{SVG_directory}/" + filename, height=desired_height)
     ```
   • (In your class constructor you can use:
     ```python
     try:
         super().__init__(path, height=desired_height, **kwargs)
     except (FileNotFoundError, OSError):
         Path(path).write_text(svg_code, encoding="utf-8")
         super().__init__(path, height=desired_height, **kwargs)
     ```
     )

3. Design language
   • Valid XML; group logically with `<g>`; avoid filters unsupported by Manim.

4. No external icon libraries
   • Every SVG must either already reside in {SVG_directory} or have just been created there.

5. Directory hygiene
   • Re‐use existing files whenever they truly match to avoid duplicates.
   • Name new files concisely as `<concept>.svg` (lowercase, unique).

Escape backslashes (`\`) and double quotes (`"`). Ensure every SVG loads without errors via `SVGMobject`.
The resulting illustration should be immediately **recognizable** (no guesswork) yet visually **clean and balanced**.