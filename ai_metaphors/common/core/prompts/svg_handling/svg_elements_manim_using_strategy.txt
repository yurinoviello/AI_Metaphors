2. Fallback source — use Manim primitives
   • If no suitable file in {SVGs} was found, you are responsible to use Manim primitive graphics directly (Circle, Rectangle, Line, Polygon, etc.) instead of SVGs.

