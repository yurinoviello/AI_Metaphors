The main logic of the pipeline is in the ```utils.py``` file,.

With the notebook you can:
    — load an example from the subset with a pre-generated metaphor
    — generate a manim script of the animation